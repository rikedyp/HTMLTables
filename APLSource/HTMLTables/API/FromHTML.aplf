 FromHTML←{
⍝ ⍵: Simple character vector or nested vector of character vectors
⍝ ←: List of APL nested matrices with text extracted from <table> tags in ⍵
     ExtractTables←'<table>'∘##.HTX
     tables←ExtractTables ⍵
     0=≢tables:''
     ⍺←⍳≢tables
     (error xml)←↓⍉↑{0::(⎕EN ⎕DMX.Message) ⋄ (0,⊂⎕XML ⍵)}¨tables[⍺]
     e←0<error
     _←{∨/e:⎕←'There were errors processing tables: ',∊(⎕UCS 13),⍕(⍸e),':',↑e/xml ⋄ ''}⍬
     ##.XMLTable2Array¨xml/⍨~e
 }
